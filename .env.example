# Server Configuration
PORT=8000
API_KEY=your-api-key-here  # Optional: remove this line to disable authentication
HOST=0.0.0.0  # Host to bind the server to

# TTS Model Configuration
# If not specified, default models will be used
# BACKBONE_REPO=neuphonic/neutts-air  # or 'neutts-air-q4-gguf' with llama-cpp-python
# BACKBONE_DEVICE=cpu  # or 'cuda' for GPU
# CODEC_REPO=neuphonic/neucodec  # or 'neuphonic/distill-neucodec', 'neuphonic/neucodec-onnx-decoder'
# CODEC_DEVICE=cpu  # or 'cuda' for GPU (not applicable for onnx decoder)

# Logging Configuration
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL